import { Button } from 'bootstrap';
import React from 'react';

const Banner = () => {
    return (
        <div className=''>
            <h1>This is banner (hulk)</h1>
        </div>
    );
};

export default Banner;